h1|{{board.name}}

.lists-container(dragng-container, callbacks="callbacks")
	.list-wrapper(ng-repeat="id in lists.order")
		{{lists.details[id].cards.length}}
		list-widget(list="lists.details[id]")

	create-list-form(board="board")