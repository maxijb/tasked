.user-icon-wrapper(title="{{user.name || user.showName}}")
	img(ng-if="user.icon", ng-src="{{user.icon}}")
	span.initials(ng-if="!user.icon")|{{initials}}

	a.user-icon-remove(ng-if="removeButton=='true'", ng-click="removeAction({user: user})")|X