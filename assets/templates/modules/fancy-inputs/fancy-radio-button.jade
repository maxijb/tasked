.fancy-radio-button
	.option(ng-repeat="item in options", ng-click="frbctrl.selectStatus(item.value)", ng-if="item.value != 'organization'")
		p.option-title|{{ item.title | i18n }}
		p.option-subtitle|{{ item.subtitle | i18n }}
